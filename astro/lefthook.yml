pre-commit:
  commands:
    biome:
      glob: "*.{js,mjs,ts,tsx,jsx,json,jsonc,astro,css}"
      run: npx biome check --write --no-errors-on-unmatched --staged {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
